{{> nav}}
    <div class="flex-container">
        {{#each data}}
        <div class="card" id="scraped-article-{{this.id}}">
            <div class="headline-container">
                <a class="headline" href="{{this.url}}" id="url-{{this.id}}" target="_blank">{{this.headline}}</a>
                <button id="save" button-id="{{this.id}}">Save</button>
            </div>
            <p class="summary" id="summary-{{this.id}}">{{this.summary}}</p>
        </div>
        {{/each}}
    </div>
